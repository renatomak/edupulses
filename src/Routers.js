const express = require("express");

const router = express.Router();

router.get("/api/v1/users/fibonacci/:parameter", (req, res) => {});
router.get("/api/v1/users/cyty/:parameter", (req, res) => {});

module.exports = router;
